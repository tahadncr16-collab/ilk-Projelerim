#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
/*
    int smallNumber,largeNumber,number,i;
    printf("progrqami durdurmak icin 0 tusuna basiniz\n");
    i=1;
    do {
        printf("%dth:",i);
        scanf("%d",&number);
        if (number==0) break;
        if (number>largeNumber) {
            largeNumber=number;
        }
        if (number<smallNumber) {
            smallNumber=number;
        }
        i++;
    }while (number !=0);
    printf("The largest number is %d \n",largeNumber);
    printf("The smallest number is %d",smallNumber);
    */
/*
    int smallNumber,largeNumber,number,i;
    printf("progrqami durdurmak icin 0 tusuna basiniz\n");
    i=1;
    while (1) {
        printf("%dth:",i);
        scanf("%d",&number);
        if (number==0) break;
        if (number>largeNumber) {
            largeNumber=number;
        }
        if (number<smallNumber) {
            smallNumber=number;
        }
        i++;
    }
    printf("The largest number is %d \n",largeNumber);
    printf("The smallest number is %d",smallNumber);
    */
/*
    int i,largeNumber,smallNumber,number;
    printf("programi sonlandirmak icin lutfen 0 giriniz\n");
    largeNumber =0;
    smallNumber =0;
    for (i=1;i>0;i++) {
        if (number==0) break;
        printf("th%d:",i);
        scanf("%d",&number);
        if (number>largeNumber) {
            largeNumber = number;
        }
        if (smallNumber>number) {
            smallNumber = number;
        }
    }
    printf("The largest number is %d \n",largeNumber);
    printf("The smallest number is %d",smallNumber);
*/
    return 0;
}
